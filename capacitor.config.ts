import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.app.floating',
  appName: 'Floating Buttons',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;

